// This service is deprecated as the application has been moved to a fully offline, local processing architecture.
// No API keys are required.

export const processWithGemini = async (text: string, instruction: string): Promise<string> => {
  return text;
};
